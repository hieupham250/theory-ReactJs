import React from "react";

export default function UseCallback() {
  return (
    <div>
      UseCallback
      {/* 
      đây là 1 hook trong react js
      cách dùng:
        import {useCallback} form "react"
        useCallback(): nhận vào 2 đối số
        1. useCallback là arrow function => đi xử lý logic
        2. [] || [dependency] => khi nào depency này thay ddoooir thì useCallback được gọi
        cũng tương tự useEffect

    */}
    </div>
  );
}
